"""App configuration for the computing module."""
from django.apps import AppConfig


class ComputingConfig(AppConfig):
    """Django app config for the GPU computing module."""
    name = "computing"
